# Backend base URL — only needed for physical devices or non-default ports.
# iOS simulator:     http://localhost:3001  (default, no override needed)
# Android emulator:  http://10.0.2.2:3001   (default, no override needed)
# Physical device:   http://<YOUR_LAN_IP>:3001  ← set this
EXPO_PUBLIC_API_URL=
